@model int?
<!-- Edit button accepts ID of the page thats edited-->
<a type="button" class="btn btn-primary btn-sm"
   href="@Url.Action("Edit", new { id = Model})">
    <span class="glyphicon glyphicon-pencil"></span>
    <span>Edit</span>
</a>